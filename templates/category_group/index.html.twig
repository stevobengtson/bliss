{% extends 'base.html.twig' %}

{% block title %}CategoryGroup index{% endblock %}

{% block body %}
    <h1>CategoryGroup index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category_group in category_groups %}
            <tr>
                <td>{{ category_group.name }}</td>
                <td>
                    <a href="{{ path('app_category_group_edit', {'id': category_group.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_category_group_new') }}">Create new</a>
{% endblock %}
