{% for account in this.accounts %}
<li>
    <a href="{{ path('app_account_transaction_index', {id: account.id}) }}" class="nav-link text-white">
        {{ account.nickName }}
    </a>
</li>
{% endfor %}
