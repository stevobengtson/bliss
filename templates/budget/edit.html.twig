{% extends 'base.html.twig' %}

{% block title %}Edit Budget{% endblock %}

{% block body %}
    {% include '_page_header.html.twig' with {
        'back_path': path('app_budget_index')
    } only %}

    <div class="container form-card">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        Edit Budget
                    </div>

                    <div class="card-body">
                        {{ include('budget/_form.html.twig', {'button_label': 'Update', 'canDelete': true}) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
