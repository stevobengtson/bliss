<form method="post" action="{{ path('app_category_group_delete', {'id': category_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_group.id) }}">
    <button class="btn">Delete</button>
</form>
